Estimate, separately for HAIKU and OPUS, the probability each answers this query CORRECTLY and how many OUTPUT TOKENS each produces, using the most similar past problems below. Compute each model's probability as the similarity-weighted fraction of neighbors that model got right, but cap your confidence by the evidence: only go above 85% or below 15% when the top 2–3 neighbors are both highly similar AND unanimous for that model, otherwise stay within 25–75%. Because both models miss problems more often than raw neighbor agreement suggests, shade every estimate above 75% downward by ~10 points and never assign 90%+ unless near-identical neighbors were essentially all correct. Treat HAIKU and OPUS independently: when stronger neighbors solved what cheaper ones missed, push OPUS up and HAIKU down so they separate, but if a low-rated HAIKU has even one or two similar correct neighbors, raise it toward 45–55% rather than leaving it near 28%. Read token counts as a difficulty gauge — long neighbor solutions mean lower HAIKU correctness and a wider OPUS edge, short ones mean both likely succeed. Predict each model's tokens as the similarity-weighted average of that model's neighbor token counts, scaled up for queries that look harder than the neighbors and down for simpler ones. When the nearest neighbors have low similarity or conflicting outcomes, compress both probabilities toward 50% since weak evidence cannot justify a confident or sharply split answer. Output exactly: HAIKU=<correct%>,<tokens> OPUS=<correct%>,<tokens>.