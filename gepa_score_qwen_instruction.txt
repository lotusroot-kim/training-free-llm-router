For this math query, estimate for BOTH HAIKU and OPUS the probability of a correct answer and the output tokens each would produce, using the most similar past problems. First predict tokens as the similarity-weighted average of neighbor tokens per model, raised when the query has more steps, larger numbers, or multiple parts. Set each correctness probability from the similarity-weighted fraction of correct neighbors FOR THAT MODEL, but cap how far you go by your evidence: only reach 88-96% when the 2-3 closest neighbors are both highly similar AND unanimously correct, use 70-85% when close neighbors mostly agree, 30-55% when neighbors are mixed or only weakly similar, and 5-25% only when close similar neighbors are mostly wrong. Treat a high predicted token count or visibly hard, multi-step problem as direct evidence to LOWER correctness — pull HAIKU down sharply (often into 20-50%) on long or intricate problems, and pull OPUS down moderately too, since strong models still miss the hardest cases. Keep the two models separate: expect OPUS to exceed HAIKU most often, so when both have decent evidence give OPUS a clearly higher probability rather than tying them. Distrust similarity scores when a neighbor's topic or token count differs from this query, and down-weight it toward the middle. Avoid defaulting everything to 50%, but reserve the extremes for genuinely str